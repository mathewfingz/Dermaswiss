  assign cities = ""
  for block in sorted_blocks
    if block.settings.language != 'all' and block.settings.language != current_lang
      continue
    endif
    
    assign city = block.settings.city | strip
    if city == blank and block.settings.location != blank
      assign city = block.settings.location | split: ',' | last | strip
    endif
    
    if city != blank
      assign city_check = city | append: "|"
      unless cities contains city_check
        assign cities = cities | append: city | append: "|"
      endunless
    endif
  endfor
  assign city_array = cities | split: "|" | sort
-%}

<div class="monthly-calendar-list container section-{{ section.id }}" 
  style="--month-font-size: {{ section.settings.month_font_size }}px; --date-title-spacing: {{ section.settings.date_title_spacing }}px;"
>
  <div class="calendar-header">
    <h1 class="h2">{{ section.settings.heading | default: 'Calendario de Eventos' }}</h1>
    {% if section.settings.description != blank %}
      <div class="calendar-description h5">{{ section.settings.description }}</div>
    {% endif %}
    
    <div class="calendar-filters">
      <div class="filter-item">
        <label for="filter-city">
          <span class="filter-icon">📍</span>
          {% if current_lang == 'es' %}Ciudad{% else %}City{% endif %}
        </label>
        <div class="select-wrapper">
          <select id="filter-city">
            <option value="">{% if current_lang == 'es' %}Todas las ciudades{% else %}All Cities{% endif %}</option>
            {% for city in city_array %}
              {% if city != blank %}
                <option value="{{ city }}">{{ city }}</option>
              {% endif %}
            {% endfor %}
          </select>
        </div>
      </div>
      <div class="filter-item">
        <label for="filter-date">
          <span class="filter-icon">📅</span>
          {% if current_lang == 'es' %}Mes{% else %}Month{% endif %}
        </label>
        <input type="month" id="filter-date">
      </div>
      <div class="filter-actions hidden" id="filter-clear-wrapper">
        <button type="button" id="filter-clear" class="btn btn--underline">
          {% if current_lang == 'es' %}Limpiar filtros{% else %}Clear filters{% endif %}
        </button>
      </div>
    </div>
  </div>

  <div class="calendar-list">
    {%- if section.blocks.size > 0 -%}
      {%- assign current_month_year = "" -%}
      {%- assign previous_date = "" -%}
      
      {%- for block in sorted_blocks -%}
        {%- if block.settings.language != 'all' and block.settings.language != current_lang -%}
          {%- continue -%}
        {%- endif -%}
        
        {%- assign event_date = block.settings.event_date -%}
        {%- assign event_month_year = event_date | date: "%B %Y" -%}
        {%- assign event_month_val = event_date | date: "%Y-%m" -%}
        
        {%- assign event_city = block.settings.city | strip -%}
        {%- if event_city == blank and block.settings.location != blank -%}
          {%- assign event_city = block.settings.location | split: ',' | last | strip -%}
        {%- endif -%}

        {%- if event_month_year != current_month_year -%}
          <div class="calendar-month-header" data-month="{{ event_month_val }}">
            <h2 class="month-title">{{ event_month_year | capitalize }}</h2>
          </div>
          {%- assign current_month_year = event_month_year -%}
        {%- endif -%}

        <div class="calendar-event-wrapper" data-city="{{ event_city }}" data-date="{{ event_month_val }}">
          {%- if event_date != previous_date -%}
            <div class="calendar-list__date-header">
              <span class="date-header__day">{{ event_date | date: "%d" }}</span>
              <div class="date-header__right">
                <span class="date-header__month">{{ event_date | date: "%B" | capitalize }}</span>
                <span class="date-header__year">{{ event_date | date: "%Y" }}</span>
              </div>
            </div>
          {%- endif -%}

          <div class="calendar-list__event" {{ block.shopify_attributes }}>
            <div class="event__color-bar" style="background-color: {{ block.settings.event_color }}"></div>
            <div class="event__content">
              <div class="event__meta">
                {% if block.settings.event_type != blank %}
                  <span class="event__type">{{ block.settings.event_type }}</span>
                {% endif %}
                {% if block.settings.price != blank %}
                  <span class="event__price">{{ block.settings.price }}</span>
                {% endif %}
              </div>
              <h3 class="event__title h4">{{ block.settings.title }}</h3>
              <div class="event__details">
                {% if block.settings.location != blank %}
                  <div class="event__location">
                    <span class="event__icon">📍</span> {{ block.settings.location }}
                  </div>
                {% endif %}
              </div>
              {% if block.settings.event_description != blank %}
                <div class="event__description rte">{{ block.settings.event_description }}</div>
              {% endif %}
              {% if block.settings.registration_link != blank %}
                <div class="event__actions">
                  <a href="{{ block.settings.registration_link }}" class="btn btn--primary btn--small" target="_blank">
                    {% if current_lang == 'es' %}Registrarse{% else %}Register Now{% endif %}
                  </a>
                </div>
              {% endif %}
            </div>
          </div>
        </div>
        
        {%- assign previous_date = event_date -%}
      {%- endfor -%}
    {%- else -%}
      <div class="calendar-list__empty text-center">
        <p>
          {% if current_lang == 'es' %}
            No hay eventos programados en este momento.
          {% else %}
            No events scheduled at this time.
          {% endif %}
        </p>
      </div>
    {%- endif -%}
  </div>
  <div class="calendar-list__no-results text-center hidden">
    <p>
      {% if current_lang == 'es' %}
        No se encontraron eventos con los filtros seleccionados.
      {% else %}
        No events found with the selected filters.
      {% endif %}
    </p>
  </div>
</div>

<style>
  .monthly-calendar-list {
    margin-top: 60px;
    margin-bottom: 60px;
    max-width: 800px;
  }
  .calendar-header {
    text-align: center;
    margin-bottom: 50px;
  }
  .calendar-description {
    margin-top: 10px;
    color: rgba(var(--color-foreground), 0.7);
  }
  .calendar-list {
    display: flex;
    flex-direction: column;
    gap: 30px;
  }
  .calendar-list__date-header {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-bottom: 15px;
    padding-bottom: 10px;
    border-bottom: 2px solid #f0f0f0;
  }
  .date-header__day {
    font-size: 3rem;
    font-weight: bold;
    line-height: 1;
    color: var(--color-primary);
  }
  .date-header__right {
    display: flex;
    flex-direction: column;
  }
  .date-header__month {
    font-weight: bold;
    text-transform: uppercase;
    font-size: 1.2rem;
    letter-spacing: 0.1rem;
  }
  .date-header__year {
    font-size: 1.1rem;
    color: #666;
  }
  .calendar-month-header {
    margin-top: 40px;
    margin-bottom: 20px;
    padding-bottom: 10px;
    border-bottom: 3px solid var(--color-primary);
  }
  .month-title {
    font-size: var(--month-font-size, 2.5rem);
    margin: 0;
    color: var(--color-primary);
  }
  .calendar-filters {
    display: flex;
    justify-content: center;
    align-items: flex-end;
    gap: 30px;
    margin-top: 40px;
    flex-wrap: wrap;
    background: #f9f9f9;
    padding: 25px;
    border-radius: 12px;
    box-shadow: inset 0 2px 4px rgba(0,0,0,0.02);
  }
  .filter-item {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 8px;
  }
  .filter-item label {
    font-weight: 700;
    font-size: 1.1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: flex;
    align-items: center;
    gap: 5px;
    color: #444;
  }
  .filter-icon {
    font-size: 1.4rem;
  }
  .filter-item select, .filter-item input {
    padding: 10px 15px;
    border: 2px solid #eee;
    border-radius: 8px;
    font-size: 1.4rem;
    min-width: 180px;
    transition: all 0.2s ease;
    background: #fff;
    cursor: pointer;
  }
  .filter-item select:hover, .filter-item input:hover {
    border-color: var(--color-primary);
  }
  .filter-item select:focus, .filter-item input:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(var(--color-primary-rgb), 0.1);
  }
  .filter-actions {
    margin-bottom: 5px;
  }
  .calendar-month-header {
    margin-top: 60px;
    margin-bottom: 25px;
    padding-bottom: 12px;
    border-bottom: 3px solid var(--color-primary);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .month-title {
    font-size: var(--month-font-size, 2.5rem);
    margin: 0;
    color: var(--color-primary);
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }
  .calendar-list__event {
    display: flex;
    background: #fff;
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.05);
    overflow: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    margin-left: 20px;
    margin-top: var(--date-title-spacing, 15px);
    border: 1px solid #f0f0f0;
  }
  .calendar-list__event:hover {
    transform: translateY(-3px) translateX(2px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.1);
  }
  .calendar-event-wrapper {
    margin-bottom: 10px;
  }
  .calendar-event-wrapper.hidden {
    display: none;
  }
  .calendar-month-header.hidden {
    display: none;
  }
  .calendar-list__no-results.hidden {
    display: none;
  }
  .filter-actions.hidden {
    display: none;
  }
</style>

<script>
  (function() {
    function initCalendarFilters() {
      const cityFilter = document.getElementById('filter-city');
      const monthFilter = document.getElementById('filter-date');
      const clearBtn = document.getElementById('filter-clear');
      const clearWrapper = document.getElementById('filter-clear-wrapper');
      const eventWrappers = document.querySelectorAll('.calendar-event-wrapper');
      const monthHeaders = document.querySelectorAll('.calendar-month-header');
      const noResults = document.querySelector('.calendar-list__no-results');

      if (!cityFilter || !monthFilter) return;

      function filterEvents() {
        const selectedCity = cityFilter.value.toLowerCase();
        const selectedMonth = monthFilter.value; // YYYY-MM
        let visibleCount = 0;

        // Show/hide clear button
        if (selectedCity || selectedMonth) {
          clearWrapper.classList.remove('hidden');
        } else {
          clearWrapper.classList.add('hidden');
        }

        // Reset month headers
        monthHeaders.forEach(header => header.classList.remove('hidden'));

        eventWrappers.forEach(wrapper => {
          const eventCity = wrapper.getAttribute('data-city').toLowerCase();
          const eventDate = wrapper.getAttribute('data-date');
          
          let cityMatch = !selectedCity || eventCity.includes(selectedCity);
          let monthMatch = !selectedMonth || eventDate === selectedMonth;

          if (cityMatch && monthMatch) {
            wrapper.classList.remove('hidden');
            visibleCount++;
          } else {
            wrapper.classList.add('hidden');
          }
        });

        // Hide month headers that have no visible events
        monthHeaders.forEach(header => {
          const monthVal = header.getAttribute('data-month');
          const visibleInMonth = document.querySelectorAll(`.calendar-event-wrapper[data-date="${monthVal}"]:not(.hidden)`);
          if (visibleInMonth.length === 0) {
            header.classList.add('hidden');
          }
        });

        if (visibleCount === 0) {
          noResults.classList.remove('hidden');
        } else {
          noResults.classList.add('hidden');
        }
      }

      function clearFilters() {
        cityFilter.value = "";
        monthFilter.value = "";
        filterEvents();
      }

      cityFilter.addEventListener('change', filterEvents);
      monthFilter.addEventListener('change', filterEvents);
      clearBtn.addEventListener('click', clearFilters);
    }

    document.addEventListener('DOMContentLoaded', initCalendarFilters);
    document.addEventListener('shopify:section:load', initCalendarFilters);
  })();
</script>

{% schema %}
{
  "name": "Calendario de Lista",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Encabezado",
      "default": "Próximos Eventos"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Descripción",
      "default": "<p>Mantente al día con nuestros próximos eventos y programas profesionales.</p>"
    },
    {
      "type": "range",
      "id": "month_font_size",
      "label": "Tamaño de fuente del mes",
      "min": 16,
      "max": 60,
      "step": 1,
      "unit": "px",
      "default": 30
    },
    {
      "type": "range",
      "id": "date_title_spacing",
      "label": "Espaciado entre fecha y título",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 15
    }
  ],
  "blocks": [
    {
      "type": "event",
      "name": "Evento",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Título del Evento",
          "default": "Nuevo Evento"
        },
        {
          "type": "text",
          "id": "event_date",
          "label": "Fecha (YYYY-MM-DD)",
          "info": "Ejemplo: 2025-12-25",
          "default": "2025-12-25"
        },
        {
          "type": "text",
          "id": "event_type",
          "label": "Tipo de Evento",
          "info": "Ej: Masterclass, Seminario, Taller",
          "default": "Webinar"
        },
        {
          "type": "text",
          "id": "price",
          "label": "Precio",
          "info": "Ej: Gratis, $50, Varía"
        },
        {
          "type": "text",
          "id": "city",
          "label": "Ciudad (para filtros)",
          "info": "Ej: Miami, Madrid, Bogotá"
        },
        {
          "type": "text",
          "id": "location",
          "label": "Lugar",
          "info": "Ej: Online, Miami, Hotel Plaza"
        },
        {
          "type": "url",
          "id": "registration_link",
          "label": "Link de registro"
        },
        {
          "type": "richtext",
          "id": "event_description",
          "label": "Descripción del Evento"
        },
        {
          "type": "color",
          "id": "event_color",
          "label": "Color de etiqueta",
          "default": "#db0032"
        },
        {
          "type": "select",
          "id": "language",
          "label": "Idioma del Evento",
          "options": [
            {
              "value": "all",
              "label": "Todos los idiomas"
            },
            {
              "value": "es",
              "label": "Español"
            },
            {
              "value": "en",
              "label": "Inglés"
            }
          ],
          "default": "all"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Calendario de Lista",
      "blocks": [
        {
          "type": "event",
          "settings": {
            "title": "Evento de Masterclass",
            "event_date": "2025-12-25",
            "event_description": "<p>Únete a nuestra masterclass sobre técnicas avanzadas de skincare.</p>",
            "event_type": "Masterclass",
            "location": "Online",
            "price": "Gratis"
          }
        }
      ]
    }
  ]
}
{% endschema %}

